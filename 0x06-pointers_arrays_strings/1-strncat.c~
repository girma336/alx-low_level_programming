#include "main.h"
#include <string.h>
/**
 *_strncat: concatinat n strings
 *@dest:char
 *@src:char
 *@n:int
 *Return:char
 */
char *_strncat(char *dest, char *src, int n)
{
return (strncat(dest, src, n));
}
