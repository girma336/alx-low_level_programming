#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 *malloc_checked-return a pointer to the memory
 *@b:unsigned int
 *Return:void
 */
void *malloc_checked(unsigned int b)
{
void *p;

p = malloc(b);

if (p == NULL)
{
exit (98);
}
return (p);
}
