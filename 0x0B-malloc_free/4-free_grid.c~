#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 *free_grid-print or return the input
 *@grid:int
 *@height:int
 *Return:void
 */
void free_grid(int **grid, int height)
{
int i;
if (grid == NULL && height = 0)
return (NULL);
for (i = 0; i < height; i++)
free(grid[i]);
free(grid);
}
