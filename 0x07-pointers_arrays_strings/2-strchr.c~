#include "main.h"
#include <stdio.h>
/**
 *_strchr-changed string to char
 *@s:char
 *@c:char
 *return:char
 */
char *_strchr(char *s, char c)
{

int i;
while (1)
{
i = *s++;
if (i == c)
{
return (s - 1);
}
if (i == 0)
{
return (NULL);
}
}
}
