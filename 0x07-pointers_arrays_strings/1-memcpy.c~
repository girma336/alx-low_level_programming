#include "main.h"
/**
 *_memcpy-copy the memset char
 *@dest:char
 *@src:char
 *@n:unsigned int 
 *Retuen: char
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
unsigned int i;

for (i = 0; i < n; i++)
{
dest[i] = src[i];
}
return (dest);
}
