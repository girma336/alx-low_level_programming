#include "main.h"
/**
 *_isalpha-check for lower case || upper case 
 *@c:input.
 *Return: 1 if c is lowercase
 *0 if c is anthing else
 */
int _isalpha(int c)
{
  if (c >= 'A' && c <=  'Z' || c >= 'a' && c <= 'z')
{
return (1);
}
 else
{
return (0);
}
}
