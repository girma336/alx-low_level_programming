#include "main.h"
/**
 *print_sign: check the sign 
 *@c: case
 *Return: if n greter than 0 +
 *and 0 is ewual to 0 
 */
int print_sign(int n)
{
if (n > 0)
{
return (1);
}
else if (n == 0)
{
return (0);
}
else
{
return (-1);
}
}
