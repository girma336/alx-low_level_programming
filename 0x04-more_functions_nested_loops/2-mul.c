#include "main.h"
#include <stdio.h>
/**
 *mul-the number.
 *@c: int.
 *Return:Always 0 (success)
 *multipllication of two number
 */
int mul(int a, int b)
{
int mult;
mult = (a * b);
return (mult);
return (0);
}
