#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
/**
 *_atoi- convert string to num
 *@s:char
 *Return:always 0
 */
int _atoi(char *s)
{
int num = atoi(s);
return (num);
return (0);
}
